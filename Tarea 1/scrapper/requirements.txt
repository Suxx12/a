# Dependencias principales del scrapper
requests==2.31.0
numpy==1.26.4
folium==0.15.1
webbrowser==0.10.0

# Dependencias estándar de la biblioteca que pueden ser necesarias
python-dateutil==2.8.2
pytz==2024.1

# Dependencias para el análisis de datos
# No hay dependencias adicionales para json_analisis.py 
# ya que solo usa bibliotecas estándar (json, os, glob, collections)