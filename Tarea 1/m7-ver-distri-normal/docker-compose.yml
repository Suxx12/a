version: '3.8'

services:
  visualizador:
    build: .
    container_name: visualizador-distribucion_normal
    volumes:
      - ./:/app/output
    environment:
      - DISPLAY=${DISPLAY}
    command: >
      bash -c "python visualizador_distribucion.py && 
              cp distribucion_normal.png /app/output/"

volumes:
  data: